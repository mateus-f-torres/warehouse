import {makeStyles} from '@material-ui/core/styles'
import {GRID} from '../useLoginStyles'

const useLoginTitleStyles = makeStyles({
  title: {
    'grid-area': GRID.title,
  },
})

export default useLoginTitleStyles
